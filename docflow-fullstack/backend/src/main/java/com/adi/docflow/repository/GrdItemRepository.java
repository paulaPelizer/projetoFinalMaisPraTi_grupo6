package com.adi.docflow.repository;

import com.adi.docflow.model.GrdItem;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GrdItemRepository extends JpaRepository<GrdItem, Long> {}
